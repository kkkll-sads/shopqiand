/**
 * OrderFilters - 订单筛选组件（当前未使用，保留用于未来扩展）
 */
import React from 'react';

interface OrderFiltersProps {
  // 预留筛选功能接口
}

const OrderFilters: React.FC<OrderFiltersProps> = () => {
  // 当前订单列表页面没有筛选功能
  // 此组件保留用于未来扩展
  return null;
};

export default OrderFilters;
