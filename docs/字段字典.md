# 字段字典文档

## 后端地址配置

### 开发环境
- **默认后端地址**: `http://127.0.0.1:8000/index.php`
- **备用地址** (已注释): `http://18.162.70.209:3005/index.php`
- **前端开发端口**: `5657`

### 配置方式
- 环境变量 `VITE_API_TARGET` 可覆盖默认地址
- API 前缀: `/api`
- 代理路径: `/uploads`, `/static`, `/storage`

---

## 核心数据类型

### 1. 用户信息 (UserInfo)

| 字段 | 类型 | 说明 | 备注 |
|------|------|------|------|
| `id` | number | 用户ID | 主键 |
| `username` | string | 用户名 | 登录账号 |
| `nickname` | string | 昵称 | 显示名称 |
| `email` | string | 邮箱 | 联系邮箱 |
| `mobile` | string | 手机号 | 登录手机号 |
| `avatar` | string | 头像 | 图片URL |
| `gender` | number | 性别 | 0=未知, 1=男, 2=女 |
| `birthday` | string\|null | 生日 | 日期格式 |
| `money` | string | 总资产 | ⚠️ 派生值(只读) |
| `balance_available` | string | 可用余额 | ✅ 核心字段 |
| `service_fee_balance` | string | 确权金 | ✅ 核心字段 |
| `withdrawable_money` | string | 可提现余额 | ✅ 核心字段 |
| `score` | number | 消费金 | ✅ 核心字段 |
| `green_power` | string\|number | 绿色算力 | 交易算力 |
| `pending_activation_gold` | number\|string | 待激活金 | 独立核算 |
| `usdt` | string | USDT余额 | 加密货币 |
| `last_login_time` | number | 最后登录时间 | 时间戳 |
| `last_login_ip` | string | 最后登录IP | IP地址 |
| `join_time` | number | 注册时间 | 时间戳 |
| `motto` | string | 个性签名 | 用户签名 |
| `draw_count` | number | 抽奖次数 | 可用次数 |
| `user_type` | number | 用户类型 | 0=普通, 1=VIP |
| `token` | string | 登录令牌 | 认证Token |
| `refresh_token` | string | 刷新令牌 | 刷新Token |
| `invite_code` | string | 邀请码 | 推广码 |
| `agent_review_status` | number | 代理商审核 | -1=未申请, 0=待审核, 1=已通过, 2=已拒绝 |
| `confirm_rights_gold` | number\|string | 待激活确权金 | 可能与pending_activation_gold相同 |
| `legacy_frozen` | number\|string | 旧资产冻结余额 | 对应PHP legacy_frozen |
| `real_name` | string | 实名姓名 | 实名认证 |
| `real_name_status` | number | 实名状态 | 认证状态 |
| `consignment_coupon` | number | 寄售券数量 | 可用券数 |

#### 资金字段说明
```
总资产 (money) = 可用余额 + 可提现余额 + 消费金 + 确权金
```

---

### 2. 藏品信息 (Product / CollectionItem)

| 字段 | 类型 | 说明 | 备注 |
|------|------|------|------|
| `id` | string\|number | 藏品ID | 主键 |
| `title` | string | 藏品标题 | 商品名称 |
| `artist` | string | 艺术家 | ❌ 已废弃 |
| `price` | number | 原价 | 商品价格 |
| `image` | string | 商品图片 | 图片URL |
| `category` | string | 分类 | 商品分类 |
| `productType` | 'shop'\|'collection' | 商品类型 | shop=消费金商城, collection=藏品商城 |
| `consignmentId` | number\|string | 寄售ID | 寄售商品对应的consignment_id |
| `reservationId` | number\|string | 申购记录ID | 对应matching_pool_id |
| `reservationStatus` | string | 申购状态 | 预约状态 |
| `sessionId` | number\|string | 场次ID | 🆕 关联场次 |
| `zoneId` | number\|string | 价格分区ID | 🆕 价格分区 |
| `session_id` | number | 关联场次ID | 🆕 数据库字段 |
| `zone_id` | number | 价格分区ID | 🆕 数据库字段 |
| `core_enterprise` | varchar(255) | 核心企业 | 🆕 新增 |
| `farmer_info` | varchar(255) | 关联农户 | 🆕 新增 |
| `asset_code` | varchar(50) | 确权编号 | 🆕 格式: 37-DATA-COLLECTION-00000001 |
| `tx_hash` | varchar(64) | 存证指纹 | 🆕 MD5哈希 |
| `owner_id` | int | 当前持有人 | 🆕 用户ID |
| `fingerprint` | varchar(64) | 存证指纹 | ❌ 已删除, 替换为tx_hash |
| `description` | text | 描述 | ❌ 已删除 |
| `supplier_name` | string | 供应商名称 | 🆕 固定值 |

---

### 3. 寄售券信息 (ConsignmentCouponItem)

| 字段 | 类型 | 说明 | 备注 |
|------|------|------|------|
| `id` | number | 主键 | 寄售券ID |
| `user_id` | number | 用户ID | 所属用户 |
| `session_id` | number | 绑定场次ID | 🆕 新增字段 |
| `zone_id` | number | 绑定价格区间ID | 🆕 新增字段 |
| `price_zone` | string | 价格区间名称 | 例如: "500元区", "1000元区" |
| `expire_time` | number | 过期时间戳 | 截止时间 |
| `status` | number | 状态 | 1=可用, 0=已使用 |
| `create_time` | number | 创建时间戳 | 创建时间 |
| `update_time` | number | 更新时间戳 | 更新时间 |

---

### 4. 寄售记录 (ConsignmentItem)

| 字段 | 类型 | 说明 | 备注 |
|------|------|------|------|
| `id` | number | 寄售商品ID | 主键 |
| `consignment_id` | number | 寄售记录ID | 关联ID |
| `session_id` | number\|string | 专场ID | 场次 |
| `title` | string | 商品标题 | 藏品名称 |
| `image` | string | 商品图片 | 图片URL |
| `price` | number | 原价 | 原始价格 |
| `consignment_price` | number | 寄售价格 | 挂牌价格 |
| `stock` | number | 库存 | 可售数量 |
| `sales` | number | 销量 | 已售数量 |
| `package_id` | number | 资产包ID | 🆕 关联资产包 |
| `package_name` | string | 资产包名称 | 🆕 资产包名 |

---

### 5. 订单信息 (Order / ShopOrderItem)

| 字段 | 类型 | 说明 | 备注 |
|------|------|------|------|
| `id` | string | 订单ID | 主键 |
| `productName` | string | 商品名称 | 订单商品 |
| `productImage` | string | 商品图片 | 图片URL |
| `price` | number | 单价 | 商品单价 |
| `quantity` | number | 数量 | 购买数量 |
| `total` | number | 总价 | 订单总额 |
| `status` | string | 状态 | 订单状态 |
| `date` | string | 日期 | 订单时间 |
| `type` | string | 类型 | product=商品, transaction=交易, delivery=提货, points=积分 |
| `subStatusIndex` | number | 子状态索引 | Tab筛选索引 |

---

### 6. 交易预约 (TradeReservation)

| 字段 | 类型 | 说明 | 备注 |
|------|------|------|------|
| `reservation_id` | number | 预约记录ID | 🆕 主键 |
| `session_id` | number | 场次ID | 🆕 关联场次 |
| `zone_id` | number | 价格分区ID | 🆕 价格分区 |
| `freeze_amount` | number | 冻结金额 | 🆕 冻结资金 |
| `power_used` | number | 消耗算力 | 🆕 使用的绿色算力 |
| `weight` | number | 权重 | 🆕 撮合权重 |
| `zone_name` | string | 分区名称 | 🆕 例如: "1000元区" |
| `product_id` | number | 商品ID | 🆕 中签后的商品 |
| `match_order_id` | number | 撮合订单ID | 🆕 撮合结果 |
| `match_time` | number | 撮合时间 | 🆕 中签时间 |

---

### 7. 资金明细 (MoneyLog)

| 字段 | 类型 | 说明 | 备注 |
|------|------|------|------|
| `id` | number | 记录ID | 主键 |
| `type` | string | 类型 | 资金类型 |
| `field_type` | string | 精确字段类型 | 🆕 从user_money_log.field_type获取 |
| `amount` | number | 金额 | 变动金额 |
| `before_value` | number | 变动前余额 | 操作前 |
| `after_value` | number | 变动后余额 | 操作后 |
| `remark` | string | 备注 | 说明 |
| `create_time` | number | 创建时间 | 时间戳 |

#### field_type 枚举值
- `money` - 可用金额 (已改为派生值)
- `withdrawable_money` - 可提现金额
- `balance_available` - 可用余额
- `service_fee_balance` - 确权金
- `static_income` - 拓展提现
- `pending_activation_gold` - 待激活确权金
- `score` - 消费金

---

### 8. 旧资产解锁 (OldAssetsUnlock)

| 字段 | 类型 | 说明 | 备注 |
|------|------|------|------|
| `unlock_status` | number | 解锁状态 | 0=未解锁, 1=已解锁 |
| `unlocked_count` | number | 已解锁次数 | 🆕 累计次数 |
| `available_quota` | number | 可用解锁资格 | 🆕 剩余资格 |
| `required_gold` | number | 每次解锁需要金额 | 固定1000.00 |
| `current_gold` | number | 当前待激活金余额 | 用户余额 |
| `can_unlock` | boolean | 是否可以解锁 | 综合判断 |
| `unlock_count` | number | 当前解锁次数 | 🆕 本次后累计 |
| `consumed_gold` | number | 消耗的待激活金 | 固定1000.00 |
| `reward_item_id` | number | 获得的藏品ID | 🆕 奖励藏品 |
| `reward_item_title` | string | 获得的藏品名称 | 🆕 藏品名 |
| `reward_item_price` | number | 藏品价值 | 🆕 固定1000.00 |
| `user_collection_id` | number | 用户藏品记录ID | 🆕 持仓记录 |
| `reward_consignment_coupon` | number | 获得的寄售券数量 | 固定1 |
| `remaining_quota` | number | 剩余可用解锁资格 | 🆕 剩余次数 |

#### 解锁条件 (unlock_conditions)

| 字段 | 类型 | 说明 |
|------|------|------|
| `has_transaction` | boolean | 是否完成过交易 |
| `transaction_count` | number | 交易次数 |
| `direct_referrals_count` | number | 直推用户总数 |
| `qualified_referrals` | number | 有交易记录的直推用户数 |
| `unlocked_count` | number | 已解锁次数 |
| `available_quota` | number | 可用解锁资格 |
| `is_qualified` | boolean | 是否满足解锁条件 |
| `messages` | array | 状态说明信息列表 |

---

## 数据库表结构变更

### 新增表

#### ba_asset_package (资产包管理表)
- 添加日期: 2025-12-26
- 用途: 管理藏品资产包

#### ba_user_consignment_coupon (寄售券明细表)
- 添加日期: 2025-12-26
- 用途: 记录用户寄售券

#### ba_user_old_assets_unlock (旧资产解锁记录表)
- 添加日期: 2025-12-27
- 用途: 记录旧资产解锁历史

### 修改表

#### ba_collection_item (藏品表)
- `+7字段`: session_id, zone_id, core_enterprise, farmer_info, asset_code, tx_hash, owner_id
- `-3字段`: fingerprint, artist, description

#### ba_collection_consignment (寄售表)
- `+2字段`: package_id, package_name

#### ba_trade_reservations (交易预约表)
- `+5字段`: zone_id, product_id, power_used, match_order_id, match_time

#### ba_user (用户表)
- `+1字段`: old_assets_unlock_count

#### ba_user_collection (用户藏品表)
- `+1字段`: is_old_asset_package

#### ba_user_money_log (资金明细表)
- `+1字段`: field_type

#### ba_price_zone_config (价格分区配置表)
- 规则调整: 每500元一个分区

---

## API 接口字段映射

### 用户模块

#### GET /api/Account/profile
**返回字段**: UserInfo (见上文用户信息表)

#### GET /api/user/consignmentCoupons
**请求参数**:
- `page`: 页码 (默认1)
- `limit`: 每页数量 (默认50, 最大100)
- `status`: 状态过滤 (1=可用, 0=已使用)

**返回字段**:
- `list`: ConsignmentCouponItem[] (见寄售券信息表)
- `total`: 总记录数
- `available_count`: 可用数量

### 藏品模块

#### GET /api/CollectionItem/detail
**新增返回字段**:
- `supplier_name`: 供应商名称
- `tx_hash`: 存证指纹 (替换fingerprint)
- `asset_code`: 确权编号
- `type`: 详情类型 (market/my)

#### POST /api/collectionItem/bidBuy (盲盒预约)
**请求参数**:
- `session_id`: 场次ID (必填)
- `zone_id`: 价格分区ID (必填)
- `extra_hashrate`: 额外加注算力 (0-50)

**返回字段**: TradeReservation (见交易预约表)

### 寄售模块

#### POST /api/collectionItem/consign
**请求参数**:
- `user_collection_id`: 用户藏品记录ID
- `price`: 寄售价格

**返回字段**:
- `coupon_used`: 使用的寄售券数量
- `coupon_remaining`: 剩余寄售券数量
- `waive_type`: 免除类型
- `rollback_reason`: 回滚原因

---

## 状态枚举值

### 订单状态 (Order Status)
- `pending_pay` - 待付款
- `pending_ship` - 待发货
- `pending_confirm` - 待收货
- `completed` - 已完成
- `cancelled` - 已取消

### 寄售状态 (Consignment Status)
- `0` - 未寄售
- `1` - 寄售中
- `2` - 已售出
- `3` - 已取消

### 实名认证状态 (Real Name Status)
- `-1` - 未申请
- `0` - 待审核
- `1` - 已通过
- `2` - 已拒绝

### 代理商审核状态 (Agent Review Status)
- `-1` - 未申请
- `0` - 待审核
- `1` - 已通过
- `2` - 已拒绝

---

## 数据格式规范

### 时间格式
- **时间戳**: Unix时间戳 (秒级)
- **日期字符串**: `YYYY-MM-DD HH:mm:ss`

### 金额格式
- **类型**: string 或 number
- **精度**: 保留2位小数
- **单位**: 元 (人民币)

### 手机号格式
- **规则**: 1[3-9]开头的11位数字
- **示例**: 13800138000

### 确权编号格式
- **格式**: `37-DATA-COLLECTION-00000001`
- **说明**: 前缀 + 序列号

### 存证指纹格式
- **类型**: MD5哈希值
- **长度**: 64位字符串

---

## 更新日志

### 2025-12-27
- 新增旧资产解锁功能相关字段
- 优化资金明细接口，新增 `field_type` 字段

### 2025-12-26
- 新增寄售券字段 `session_id`, `zone_id`
- 藏品表新增多个字段，删除部分废弃字段
- 盲盒预约接口重构，统一使用场次+分区模式

---

**文档版本**: v2.5  
**更新时间**: 2026-01-06  
**维护者**: 前端开发团队
